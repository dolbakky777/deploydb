<h1><%=t("controllers.names.user.edit") %></h1>

<%= form_for @user do |form| %>
    <%= render "form", form: form %>
    <p><%= form.submit t("buttons.submit2") %></p>
<% end %>

<p><%= link_to t("messages.user.edit.message1"), @user %></p>

