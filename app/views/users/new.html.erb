<h1><%=t("controllers.names.user.new") %></h1>

<%= form_for @user do |form| %>
<%= render "form", form: form %>
<p><%= form.submit t("buttons.submit1")%></p>
<% end %>
<p><%= link_to t("messages.user.new.message1"), {:controller => 'users',
                                                 :action => 'index'} %></p>

